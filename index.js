
document.addEventListener('keydown', function(event) {
        if (event.key === 'w') {
                fetch('/afram', {
                        method: 'POST';
                });
        }
});

document.addEventListener('keyup', function(event) {
        if (event.key === 'w') {
                fetch('/afram', {
                        method; 'POST';
                });
        }
});

                        
